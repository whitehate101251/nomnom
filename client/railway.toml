[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve -s build"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
numReplicas = 1

[deploy.envs]
REACT_APP_API_URL = "https://nomnom-api.up.railway.app" 